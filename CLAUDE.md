# Operator Notes

- Follow the repo-level `AGENTS.md` instructions first.
- The active MVP is JSON-first and static-first:
  checked-in site config under `sites/**`, active listing data in `data/listings.json`, and static artifacts in `dist/sites/<site-id>`.
- Use the current closeout docs before changing workflow assumptions:
  `docs/PLAN.md`, `docs/IMPLEMENTATION_TRACKER.md`, `docs/SITE_CONFIG_REFACTOR_QA_CHECKLIST.md`.
- Treat hosted auth, database-backed submissions, and paid placement workflows as later hosted-lane work.
