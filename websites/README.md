# Reference-Only Directory

This folder is not part of the active starter pipeline.

Historical examples that used to live here have been moved under:

- `_archive/reference-websites/**`

Use `data/websites.json` and `sites/**` for the active starter instead.
