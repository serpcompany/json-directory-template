# Legacy Website Corpus

This folder is no longer the active starter source of truth.

Current active listing data lives in:

- `data/listings.json`

This `packages/content/data/websites/**` corpus is kept as reference-only legacy material for now.

Use it only when:

- reviewing older content patterns
- planning a future migration
- comparing legacy authored entries against the current JSON-first model

Do not treat new files here as part of the normal starter authoring flow unless a future migration explicitly restores that contract.
